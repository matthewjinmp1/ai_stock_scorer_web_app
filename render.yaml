services:
  - type: web
    name: ai-stock-scorer
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT --chdir src/web app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.6
