services:
  - type: web
    name: ai-stock-scorer
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python run_gunicorn.py
    envVars:
      - key: PYTHONPATH
        value: ${PWD}
      - key: PYTHON_VERSION
        value: 3.9.6
