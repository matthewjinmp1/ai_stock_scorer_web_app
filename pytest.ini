[pytest]
testpaths = web_app_development/tests
python_files = test_*.py
python_classes = TestCase
python_functions = test_*
# Show 15 slowest tests at end (override with --durations=0 for all, or --durations=N)
addopts = -v --tb=short -n 12 --durations=15
